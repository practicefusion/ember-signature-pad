<canvas
  height={{this.height}}
  width={{this.width}}

  ...attributes

  {{did-insert (action this.registerCanvasElement)}}
  {{did-insert (action this.draw)}}

  {{on "mousedown" (action this.penDown)}}
  {{on "touchstart" (action this.penDown)}}

  {{on "mousemove" (action this.penMove)}}
  {{on "touchmove" (action this.penMove)}}

  {{on "mouseup" (action this.penUp)}}
  {{on "touchend" (action this.penUp)}}
/>
